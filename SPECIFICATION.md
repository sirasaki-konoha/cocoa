# 7レーン音楽ゲーム 仕様書 v0.1

## 1. ゲーム概要

本作は、PCキーボードで操作する **7レーン縦落ち音楽ゲーム**。
画面中央に7つのレーンが表示され、楽曲に合わせて上から流れてくるノーツを、対応するキーでタイミングよく叩く。

背景では楽曲に対応したMV/BGAが再生され、ぼかし加工された状態で流れる。
プレイヤーはノーツを正確に叩いてスコアを稼ぎ、特殊な水色ノーツを叩くことでスコア倍率ボーナスを得る。

---

## 2. 対応プラットフォーム

初期開発対象は **PC**。

推奨開発環境：

* Unity
* C#
* Windows向けビルドを優先
* 将来的にWebGLやmacOS対応も検討可能

---

## 3. 基本画面構成

画面構成は以下の通り。

```text
┌────────────────────────────┐
│                            │
│     ぼかされたBGA / MV背景   │
│                            │
│        ┌──────────┐        │
│        │ 7レーン   │        │
│        │          │        │
│        │ ノーツ落下 │        │
│        │          │        │
│        │ 判定ライン │        │
│        └──────────┘        │
│                            │
│ スコア / コンボ / 判定表示   │
└────────────────────────────┘
```

中央に7レーンを配置する。
背景にはBGAを表示し、視認性を上げるために以下の処理を行う。

* 背景動画をぼかす
* 背景を少し暗くする
* レーン部分に半透明の黒いパネルを重ねる
* ノーツと判定ラインを見やすくする

---

## 4. レーン仕様

レーンは左から右へ以下の順番で配置する。

| レーン番号 | キー | ノーツ色 | 位置  |
| ----: | -- | ---- | --- |
|     1 | A  | 白    | 一番左 |
|     2 | W  | 緑    | Aの右 |
|     3 | E  | 白    | Wの右 |
|     4 | F  | 緑    | Eの右 |
|     5 | J  | 白    | Fの右 |
|     6 | I  | 緑    | Jの右 |
|     7 | O  | 白    | 一番右 |

キー配置は以下。

```text
A   W   E   F   J   I   O
白  緑  白  緑  白  緑  白
```

通常ノーツの色はレーンごとに固定。
ただし、特殊ノーツである水色ノーツはどのレーンにも出現する。

---

## 5. ノーツ仕様

## 5.1 通常ノーツ

通常ノーツは、対応するレーンのキーを判定ライン付近で押すことで処理される。

通常ノーツの色：

* A / E / J / O：白
* W / F / I：緑

通常ノーツを叩くと、判定に応じてスコアが加算される。

---

## 5.2 水色ノーツ

水色ノーツは特殊ノーツ。
7レーンのどこかにランダム、または譜面データ上の指定位置で出現する。

水色ノーツを正しく叩くと、現在のスコアが **1.2倍** になる。

例：

```text
現在スコア：10,000
水色ノーツ成功後：12,000
```

水色ノーツの仕様：

| 項目    | 内容             |
| ----- | -------------- |
| 色     | 水色             |
| 出現レーン | 7レーンのいずれか      |
| 入力キー  | 出現したレーンに対応するキー |
| 成功効果  | 現在スコア × 1.2    |
| 失敗効果  | 通常MISS扱い、倍率なし  |

注意点として、水色ノーツは強力なので、出しすぎるとスコアバランスが壊れやすい。
初期仕様では、1曲に数個程度から調整する。

---

## 6. 入力仕様

使用キーは以下。

```text
A W E F J I O
```

各キーはそれぞれのレーンに対応する。

| キー | レーン |
| -- | --- |
| A  | 1   |
| W  | 2   |
| E  | 3   |
| F  | 4   |
| J  | 5   |
| I  | 6   |
| O  | 7   |

キーを押した瞬間に、そのレーン内で最も判定ラインに近い未処理ノーツを検索する。

ノーツが判定範囲内にあれば成功判定。
判定範囲外であれば空打ち扱い。

---

## 7. 判定仕様

ノーツの判定は、楽曲の現在時刻とノーツの指定時刻の差で決める。

```text
判定差 = 入力時刻 - ノーツ時刻
```

初期案の判定幅：

| 判定      |       時間差 | 内容   |
| ------- | --------: | ---- |
| PERFECT |   ±50ms以内 | 最高判定 |
| GREAT   |  ±100ms以内 | 良判定  |
| GOOD    |  ±150ms以内 | 可判定  |
| MISS    | ±150msより外 | 失敗   |

初期実装では、まずは以下の3判定でもよい。

| 判定      |    時間差 |
| ------- | -----: |
| PERFECT |  ±50ms |
| GREAT   | ±100ms |
| MISS    |   それ以外 |

あとからGOODを追加しても問題ない。

---

## 8. スコア仕様

通常ノーツの基本スコア案：

| 判定      | 加算スコア |
| ------- | ----: |
| PERFECT | +1000 |
| GREAT   |  +700 |
| GOOD    |  +300 |
| MISS    |    +0 |

水色ノーツ成功時は、通常の加算ではなく、または加算後に現在スコアを1.2倍する。

初期仕様では以下にする。

```text
水色ノーツ成功時：
現在スコア = 現在スコア × 1.2
```

小数点が出た場合は整数に丸める。

```text
score = Mathf.RoundToInt(score * 1.2f);
```

---

## 9. コンボ仕様

ノーツ成功時にコンボが増える。
MISS時にコンボが0に戻る。

| 判定      | コンボ  |
| ------- | ---- |
| PERFECT | +1   |
| GREAT   | +1   |
| GOOD    | +1   |
| MISS    | 0に戻る |

初期段階ではコンボ倍率はなし。
後からスコア倍率や演出に使う。

---

## 10. ノーツの流れ方

ノーツは上から下に流れる。
判定ラインはレーン下部に配置する。

ノーツの位置は、毎フレーム単純に移動させるのではなく、**楽曲の現在時刻から逆算して表示位置を決める**。

これにより、処理落ちやフレームレートの違いによる音ズレを減らす。

基本計算：

```text
ノーツまでの残り時間 = ノーツ時刻 - 現在の曲時間
ノーツのY座標 = 判定ラインY + 残り時間 × スクロール速度
```

---

## 11. 譜面データ仕様

譜面データはJSON形式で管理する。

例：

```json
{
  "songTitle": "Sample Song",
  "artist": "Sample Artist",
  "audioFile": "sample.mp3",
  "bgaFile": "sample.mp4",
  "offset": 0.0,
  "notes": [
    {
      "time": 1.25,
      "lane": 0,
      "type": "normal"
    },
    {
      "time": 2.00,
      "lane": 3,
      "type": "bonus"
    }
  ]
}
```

各項目の意味：

| 項目        | 内容               |
| --------- | ---------------- |
| songTitle | 曲名               |
| artist    | アーティスト名          |
| audioFile | 音楽ファイル           |
| bgaFile   | 背景動画ファイル         |
| offset    | 音ズレ調整値           |
| notes     | ノーツ一覧            |
| time      | 曲開始から何秒後に叩くか     |
| lane      | レーン番号。0〜6        |
| type      | normal または bonus |

laneは以下のように対応する。

| lane | キー |
| ---: | -- |
|    0 | A  |
|    1 | W  |
|    2 | E  |
|    3 | F  |
|    4 | J  |
|    5 | I  |
|    6 | O  |

---

## 12. BGA / MV仕様

楽曲プレイ中、背景で動画を再生する。

BGA仕様：

| 項目   | 内容                |
| ---- | ----------------- |
| 表示位置 | 画面全体              |
| 表示方法 | 背景として再生           |
| 加工   | ぼかし、暗めのフィルター      |
| 音声   | 基本的にはBGA側の音声は使わない |
| 同期   | 楽曲開始と同時に再生        |

音声は音楽ファイル側をメインにする。
BGAは映像だけ使う方が音ズレ管理が楽。

---

## 13. UI仕様

プレイ画面に表示するもの：

* スコア
* コンボ数
* 判定表示
* レーン
* 判定ライン
* ノーツ
* 楽曲タイトル
* 残り時間、または進行バー

判定表示例：

```text
PERFECT
GREAT
GOOD
MISS
```

水色ノーツ成功時は専用演出を入れる。

例：

```text
SCORE ×1.2!
```

---

## 14. リザルト画面仕様

曲終了後にリザルト画面を表示する。

表示項目：

* 最終スコア
* 最大コンボ
* PERFECT数
* GREAT数
* GOOD数
* MISS数
* 水色ノーツ成功数
* ランク

ランク案：

| ランク | 条件         |
| --- | ---------- |
| S   | 高スコア、MISS少 |
| A   | 良好         |
| B   | 普通         |
| C   | クリアギリギリ    |
| D   | 失敗気味       |

初期実装では、単純にスコアでランクを決める。

---

## 15. 技術方針

開発エンジンはUnityを想定。

主なクラス構成案：

| クラス名         | 役割         |
| ------------ | ---------- |
| GameManager  | ゲーム全体の進行管理 |
| SongManager  | 楽曲再生、曲時間管理 |
| NoteManager  | ノーツ生成、管理   |
| Note         | ノーツ単体の情報   |
| InputManager | キー入力処理     |
| JudgeManager | 判定処理       |
| ScoreManager | スコア、コンボ管理  |
| BgaManager   | 背景動画再生     |
| ChartLoader  | 譜面JSON読み込み |
| UIManager    | スコアや判定表示   |

重要なのは、**楽曲時間を基準にすべてを動かすこと**。

---

# 実装順ステップ

## Step 1：Unityプロジェクト作成

まずは2Dプロジェクトとして作る。

やること：

* Unityで新規2Dプロジェクト作成
* 画面解像度を仮設定
* メインシーン作成
* カメラ設定
* Canvas作成

この段階ではゲームとして動かなくてOK。

---

## Step 2：7レーンを画面に表示する

中央に7つのレーンを並べる。

やること：

* レーン背景を7本作る
* 各レーンの下にキー名を表示
* A W E F J I O の順に並べる
* 判定ラインを下部に表示する

この時点で、音ゲーっぽい見た目を先に作る。

---

## Step 3：キー入力を確認する

各キーを押したら、対応レーンが光るようにする。

やること：

* Aキー入力
* Wキー入力
* Eキー入力
* Fキー入力
* Jキー入力
* Iキー入力
* Oキー入力
* 押したレーンに簡単な発光演出

この段階ではノーツがなくてもよい。
入力が正しく取れることを確認する。

---

## Step 4：ノーツを1つだけ流す

まずは1レーンに1つのノーツを出す。

やること：

* ノーツPrefabを作る
* 指定レーンにノーツを生成する
* 上から下に流す
* 判定ラインに向かって移動させる

最初は時間同期なしで、単純に落としてもOK。
あとで楽曲時間ベースに直す。

---

## Step 5：楽曲時間ベースの移動に変更する

音ゲーとして一番重要な部分。

やること：

* AudioSourceで曲を再生する
* `AudioSource.time` から現在時刻を取得
* ノーツの表示位置を現在時刻から計算する
* フレームレートに依存しない移動にする

この時点で、かなり本物の音ゲーに近づく。

---

## Step 6：判定処理を作る

キーを押したときに、近いノーツを判定する。

やること：

* レーンごとに未処理ノーツを管理
* キー入力時に一番近いノーツを探す
* 時間差でPERFECT/GREAT/MISSを判定
* 判定済みノーツを消す
* 判定テキストを表示する

まずは3判定でよい。

```text
PERFECT
GREAT
MISS
```

---

## Step 7：スコアとコンボを実装する

判定に応じてスコアを増やす。

やること：

* PERFECTで+1000
* GREATで+700
* MISSで+0
* 成功時コンボ+1
* MISS時コンボ0
* UIにスコアとコンボを表示

ここまでで、最低限遊べる状態になる。

---

## Step 8：譜面JSONを読み込む

手打ちでノーツを置くのをやめて、譜面データから生成する。

やること：

* JSONファイルを作る
* 曲名、音声ファイル、BGAファイル、ノーツ情報を書く
* UnityでJSONを読み込む
* 指定時間に合わせてノーツを表示する

最初の譜面は短くてOK。

---

## Step 9：水色ノーツを実装する

特殊ノーツを追加する。

やること：

* `type: "bonus"` のノーツを水色で表示
* 水色ノーツ成功時にスコアを1.2倍する
* 成功演出を表示する
* MISS時は通常MISS扱いにする

ここでゲームの個性が出る。

---

## Step 10：BGA / MV背景を実装する

背景動画を流す。

やること：

* VideoPlayerを使う
* 曲開始と同時にBGAを再生
* 背景をぼかす
* 背景を暗くする
* レーンとノーツが見やすいか確認する

最初はぼかしなしでもOK。
暗い半透明パネルだけでもかなり見やすくなる。

---

## Step 11：リザルト画面を作る

曲が終わったら結果を表示する。

やること：

* 曲終了を検知
* 最終スコア表示
* 最大コンボ表示
* PERFECT/GREAT/GOOD/MISS数表示
* ランク表示
* リトライボタン
* 曲選択に戻るボタン

---

## Step 12：オプション設定を追加する

プレイヤーごとの調整を入れる。

優先度が高い設定：

* 音ズレ調整 offset
* ノーツ速度
* BGA明るさ
* BGAぼかし強度
* キー設定

音ゲーでは特に **音ズレ調整** が重要。

---

## Step 13：譜面エディタを作る、または外部ツールで代用する

最初から譜面エディタを作る必要はない。
最初はJSONを手書きで作り、ゲーム本体が安定してからエディタを考える。

譜面エディタで必要になるもの：

* 曲を再生、一時停止
* タイムライン表示
* レーンごとにノーツ配置
* 水色ノーツ配置
* JSON出力
* オフセット調整

---

## Step 14：演出を強化する

ゲームとして気持ちよくする工程。

追加したい演出：

* キーを押したときのレーン発光
* ノーツヒット時のエフェクト
* PERFECT時の強めの演出
* 水色ノーツ成功時の特殊演出
* コンボ数が増えたときのアニメーション
* BGAとレーンのなじませ
* 曲開始前カウントダウン

---

## Step 15：テストと調整

最後に遊びやすさを調整する。

確認すること：

* 音とノーツがズレていないか
* 判定が厳しすぎないか
* ノーツ速度が見やすいか
* 水色ノーツが強すぎないか
* レーンの色が見やすいか
* BGAでノーツが見づらくないか
* 低スペックPCでも動くか

---

# 最初に作るべき完成形

最初の目標はこれ。

```text
1曲だけ遊べる
7レーンがある
A W E F J I Oで叩ける
ノーツが曲に合わせて流れる
PERFECT / GREAT / MISSが出る
スコアとコンボが増える
水色ノーツでスコアが1.2倍される
曲が終わったらリザルトが出る
```

ここまでできたら、もう「音ゲーの核」は完成している。

そのあとに、

```text
BGA
演出
曲選択
譜面エディタ
設定画面
高難易度譜面
```

を足していく
個人的には、最初のプロトタイプは **BGAなし・1曲・1譜面・3判定** で作るのが良い。
音ゲーは見た目より先に「叩いて気持ちいいか」が命なので、まず判定と同期を固めるのが大事。

